# Post-Stratification 0.2.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the 0.2.0 single-source post-stratification workflow while preserving the existing 0.1 raking API and behavior.

**Architecture:** `wf_target_population()` gains an optional joint-cell target table. New post-stratification code lives in focused `R/poststrat-*.R` files and returns the existing `wf_weights` class with extra audit fields. Diagnostics and printing branch on `provenance$method` so raking remains stable.

**Tech Stack:** Base R package, S3 classes, roxygen2, testthat, `R CMD build`, `R CMD check`.

---

## File Structure

- Modify `DESCRIPTION`: bump package version to `0.2.0`.
- Modify `NAMESPACE`: generated by roxygen2 after exports are added.
- Modify `R/target.R`: add `keep_joint` to `wf_target_population()`.
- Modify `R/utils.R`: allow `.wf_new_target(..., joint = NULL)` and keep target validation unchanged for margins.
- Create `R/poststrat-utils.R`: cell-key helpers, argument validation, ladder application.
- Create `R/poststrat-ladder.R`: `wf_collapse_ladder()`.
- Create `R/poststrat-plan.R`: resolution engine, `wf_plan_poststrat()`, print and summary helpers.
- Create `R/poststrat.R`: `wf_poststrat()` executor.
- Modify `R/rake.R`: update `print.wf_weights()` without breaking raking output.
- Modify `R/diagnostics.R`: support post-stratified `wf_weights` that do not have raking iteration columns.
- Modify `README.md`: add a compact post-stratification example.
- Modify `tests/testthat/helper-fixtures.R`: add the shared post-stratification fixture.
- Add `tests/testthat/test-poststrat-target.R`.
- Add `tests/testthat/test-poststrat-ladder.R`.
- Add `tests/testthat/test-poststrat-plan.R`.
- Add `tests/testthat/test-poststrat-exec.R`.
- Modify `tests/testthat/test-rake-diagnostics.R`: raking regression for print/diagnose compatibility.
- Modify `tests/testthat/test-data-policy.R`: staged-file/package-build audit remains private-data safe.

---

### Task 1: Joint Target Storage

**Files:**
- Modify: `R/utils.R`
- Modify: `R/target.R`
- Test: `tests/testthat/test-poststrat-target.R`
- Test: `tests/testthat/test-dims-target.R`

- [ ] **Step 1: Write the failing tests**

Create `tests/testthat/test-poststrat-target.R` with:

```r
test_that("population target can retain joint cells without changing margins", {
  fixture <- make_weightflow_fixture()

  target <- wf_target_population(
    pop = fixture$pop,
    key_map = c(gender = "gender", age = "age"),
    count = "count",
    dims = fixture$dims,
    by = "province",
    keep_joint = TRUE
  )

  expect_s3_class(target, "wf_target")
  expect_named(target$joint, c("A", "B"))
  expect_equal(names(target$joint$A), c("gender", "age", "pop"))
  expect_equal(sum(target$joint$A$pop), target$groups$A$total)
  expect_equal(sum(target$joint$B$pop), target$groups$B$total)
  expect_equal(
    unname(tapply(target$joint$A$pop, target$joint$A$gender, sum)["female"]),
    unname(target$groups$A$margins$gender["female"])
  )
})

test_that("population target omits joint cells by default", {
  fixture <- make_weightflow_fixture()

  target <- wf_target_population(
    pop = fixture$pop,
    key_map = c(gender = "gender", age = "age"),
    count = "count",
    dims = fixture$dims,
    by = "province"
  )

  expect_null(target$joint)
})

test_that("joint cells follow target scaling", {
  fixture <- make_weightflow_fixture()

  target <- wf_target_population(
    pop = fixture$pop,
    key_map = c(gender = "gender", age = "age"),
    count = "count",
    dims = fixture$dims,
    by = "province",
    scale = "custom",
    totals = c(A = 20, B = 30),
    keep_joint = TRUE
  )

  expect_equal(sum(target$joint$A$pop), 20)
  expect_equal(sum(target$joint$B$pop), 30)
  expect_equal(target$groups$A$total, 20)
  expect_equal(target$groups$B$total, 30)
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```bash
Rscript -e 'devtools::test(filter = "poststrat-target")'
```

Expected: failure because `wf_target_population()` does not accept `keep_joint`.

- [ ] **Step 3: Implement the minimal target changes**

In `R/utils.R`, change `.wf_new_target()` to accept the optional joint field:

```r
.wf_new_target <- function(mode, by, dims, groups, meta = list(), joint = NULL) {
  tgt <- structure(
    list(mode = mode, by = by, dims = dims, groups = groups, meta = meta, joint = joint),
    class = "wf_target"
  )
  .wf_validate_target(tgt)
  tgt
}
```

In `R/target.R`, add `keep_joint = FALSE` to the signature and roxygen. While
building each group, aggregate joint cells when requested:

```r
joint <- if (keep_joint) list() else NULL
for (g in sort(unique(gkey))) {
  sel <- gkey == g
  margins <- list()
  for (d in dvars) {
    v <- .chr(pop[[key_map[[d]]]][sel])
    m <- tapply(cnt[sel], v, sum)
    margins[[d]] <- .wf_margin_vector(m[m > 0])
  }
  groups[[g]] <- list(total = sum(cnt[sel]), margins = margins)

  if (keep_joint) {
    j <- data.frame(
      lapply(dvars, function(d) .chr(pop[[key_map[[d]]]][sel])),
      stringsAsFactors = FALSE,
      check.names = FALSE
    )
    names(j) <- dvars
    j$pop <- cnt[sel]
    form <- stats::as.formula(paste("pop ~", paste(dvars, collapse = " + ")))
    j <- stats::aggregate(form, data = j, FUN = sum)
    joint[[g]] <- j[j$pop > 0, c(dvars, "pop"), drop = FALSE]
  }
}
```

After `.wf_scale_groups()`, scale joint cells to the same group totals:

```r
if (keep_joint) {
  for (g in names(joint)) {
    f <- groups[[g]]$total / sum(joint[[g]]$pop)
    joint[[g]]$pop <- joint[[g]]$pop * f
  }
}
```

Pass `joint = joint` into `.wf_new_target()`.

- [ ] **Step 4: Run tests to verify they pass**

Run:

```bash
Rscript -e 'devtools::test(filter = "poststrat-target|dims-target")'
```

Expected: target tests pass with 0 failures.

- [ ] **Step 5: Commit**

```bash
git add R/utils.R R/target.R tests/testthat/test-poststrat-target.R tests/testthat/test-dims-target.R
git commit -m "feat: add joint population targets"
```

---

### Task 2: Collapse Ladder

**Files:**
- Create: `R/poststrat-utils.R`
- Create: `R/poststrat-ladder.R`
- Test: `tests/testthat/test-poststrat-ladder.R`

- [ ] **Step 1: Write the failing tests**

Create `tests/testthat/test-poststrat-ladder.R` with:

```r
test_that("collapse ladder validates dimensions and cumulative source categories", {
  dims <- wf_dims(age = c("1", "2", "3", "4", "5"), edu = c("L", "M", "H"))

  ladder <- wf_collapse_ladder(
    dims,
    level1 = list(age = c("4" = "45", "5" = "45")),
    level2 = list(age = c("1" = "123", "2" = "123", "3" = "123", "45" = "45"))
  )

  expect_s3_class(ladder, "wf_collapse_ladder")
  expect_equal(ladder$dims, c("age", "edu"))
  expect_equal(ladder$n_levels, 2)

  mat <- matrix(
    c("4", "L", "5", "H", "2", "M"),
    ncol = 2,
    byrow = TRUE,
    dimnames = list(NULL, c("age", "edu"))
  )
  expect_equal(weightflow:::.wf_apply_ladder(mat, ladder, 1)[, "age"], c("45", "45", "2"))
  expect_equal(weightflow:::.wf_apply_ladder(mat, ladder, 2)[, "age"], c("45", "45", "123"))
})

test_that("collapse ladder rejects unknown dimensions and typo categories", {
  dims <- wf_dims(age = c("1", "2"), edu = c("L", "H"))

  expect_error(
    wf_collapse_ladder(dims, level1 = list(region = c("A" = "B"))),
    class = "wf_error_input"
  )
  expect_error(
    wf_collapse_ladder(dims, level1 = list(age = c("3" = "2"))),
    class = "wf_error_input"
  )
})

test_that("cell keys split back into dimension columns", {
  mat <- matrix(c("female", "young", "male", "old"), ncol = 2, byrow = TRUE)
  colnames(mat) <- c("gender", "age")

  key <- weightflow:::.wf_cell_key(mat, c("gender", "age"))
  out <- weightflow:::.wf_split_key(key, c("gender", "age"))

  expect_equal(out$gender, c("female", "male"))
  expect_equal(out$age, c("young", "old"))
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```bash
Rscript -e 'devtools::test(filter = "poststrat-ladder")'
```

Expected: failure because `wf_collapse_ladder()` and helpers are missing.

- [ ] **Step 3: Implement ladder utilities**

Create `R/poststrat-utils.R` with these internal helpers from
`inst/reference/weightflow_poststrat.R`, preserving base-R behavior:

```r
.wf_cell_key <- function(mat, dvars) {
  do.call(paste, c(lapply(dvars, function(d) mat[, d]), sep = "\r"))
}

.wf_split_key <- function(keys, dvars) {
  parts <- do.call(rbind, strsplit(keys, "\r", fixed = TRUE))
  df <- as.data.frame(parts, stringsAsFactors = FALSE)
  names(df) <- dvars
  df
}

.wf_apply_ladder <- function(mat, ladder, lv) {
  if (lv == 0) {
    return(mat)
  }
  for (k in seq_len(lv)) {
    step <- ladder$steps[[k]]
    for (d in names(step)) {
      map <- step[[d]]
      col <- mat[, d]
      hit <- !is.na(col) & col %in% names(map)
      col[hit] <- map[col[hit]]
      mat[, d] <- col
    }
  }
  mat
}
```

Create `R/poststrat-ladder.R` with roxygen and the validated constructor:

```r
wf_collapse_ladder <- function(dims, ...) {
  if (!inherits(dims, "wf_dims")) {
    wf_abort("`dims` must be a wf_dims object.", "wf_error_input")
  }
  steps <- list(...)
  if (length(steps) > 0 && (is.null(names(steps)) || any(names(steps) == ""))) {
    wf_abort("Ladder levels must be named (for example, level1 = list(...)).", "wf_error_input")
  }
  dvars <- names(dims$vars)
  universe <- lapply(dvars, function(d) dims$vars[[d]])
  names(universe) <- dvars

  for (lv in seq_along(steps)) {
    step <- steps[[lv]]
    bad_d <- setdiff(names(step), dvars)
    if (length(bad_d) > 0) {
      wf_abort(
        sprintf("Ladder level '%s' references unknown dimension(s): %s", names(steps)[lv], paste(bad_d, collapse = ", ")),
        "wf_error_input",
        list(level = names(steps)[lv], dims = bad_d)
      )
    }
    for (d in names(step)) {
      map <- .chr(step[[d]])
      names(map) <- .chr(names(step[[d]]))
      if (is.null(names(map))) {
        wf_abort(sprintf("Ladder level '%s', dim '%s': map must be a named vector.", names(steps)[lv], d), "wf_error_input")
      }
      if (!is.null(universe[[d]])) {
        unknown <- setdiff(names(map), universe[[d]])
        if (length(unknown) > 0) {
          wf_abort(
            sprintf("Ladder level '%s', dim '%s': source categories not present at this level: %s", names(steps)[lv], d, paste(unknown, collapse = ", ")),
            "wf_error_input",
            list(level = names(steps)[lv], dim = d, unknown = unknown)
          )
        }
        kept <- setdiff(universe[[d]], names(map))
        universe[[d]] <- unique(c(kept, unname(map)))
      } else {
        universe[[d]] <- unique(unname(map))
      }
      step[[d]] <- map
    }
    steps[[lv]] <- step
  }

  structure(list(dims = dvars, steps = steps, n_levels = length(steps)), class = "wf_collapse_ladder")
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run:

```bash
Rscript -e 'devtools::test(filter = "poststrat-ladder")'
```

Expected: collapse ladder tests pass with 0 failures.

- [ ] **Step 5: Commit**

```bash
git add R/poststrat-utils.R R/poststrat-ladder.R tests/testthat/test-poststrat-ladder.R
git commit -m "feat: add poststrat collapse ladders"
```

---

### Task 3: Post-Stratification Planner

**Files:**
- Modify: `R/poststrat-utils.R`
- Create: `R/poststrat-plan.R`
- Modify: `tests/testthat/helper-fixtures.R`
- Test: `tests/testthat/test-poststrat-plan.R`

- [ ] **Step 1: Write the failing tests**

Append this shared fixture to `tests/testthat/helper-fixtures.R`:

```r
make_poststrat_fixture <- function() {
  dims <- wf_dims(gender = c("female", "male"), age = c("young", "old"))
  pop <- data.frame(
    province = "A",
    gender = c("female", "male", "female", "male"),
    age = c("young", "young", "old", "old"),
    count = c(50, 50, 50, 50),
    stringsAsFactors = FALSE
  )
  sample <- data.frame(
    id = paste0("r", 1:10),
    province = "A",
    gender = c(rep("female", 5), rep("male", 5)),
    age = c(rep("young", 4), "old", rep("young", 4), "old"),
    stringsAsFactors = FALSE
  )
  target <- wf_target_population(
    pop,
    key_map = c(gender = "gender", age = "age"),
    count = "count",
    dims = dims,
    by = "province",
    keep_joint = TRUE
  )
  ladder <- wf_collapse_ladder(dims, level1 = list(age = c("young" = "all", "old" = "all")))
  list(sample = sample, target = target, dims = dims, ladder = ladder)
}
```

Create `tests/testthat/test-poststrat-plan.R` with:

```r
test_that("poststrat planner requires a joint target", {
  fixture <- make_weightflow_fixture()
  ladder <- wf_collapse_ladder(fixture$dims)

  expect_error(
    wf_plan_poststrat(fixture$sample, fixture$target, min_cell = 2, ladder = ladder),
    class = "wf_error_schema"
  )
})

test_that("poststrat planner resolves sparse cells using the ladder", {
  fixture <- make_poststrat_fixture()

  plan <- wf_plan_poststrat(
    fixture$sample,
    fixture$target,
    min_cell = 2,
    ladder = fixture$ladder,
    granularity = "adaptive"
  )

  expect_s3_class(plan, "wf_poststrat_plan")
  expect_equal(nrow(plan$plan), 4)
  expect_true(all(c("group", "gender", "age", "pop", "n_sample", "ladder_level", "resolved_cell", "orphan") %in% names(plan$plan)))
  expect_true(any(plan$plan$ladder_level == 1))
  expect_equal(plan$diagnostics$n_cells_raw, 4)
  expect_equal(plan$diagnostics$n_orphan, 0)
})

test_that("poststrat planner flags unsupported cells when requested", {
  fixture <- make_poststrat_fixture()
  fixture$sample <- fixture$sample[fixture$sample$gender == "female", ]

  plan <- wf_plan_poststrat(
    fixture$sample,
    fixture$target,
    min_cell = 1,
    ladder = fixture$ladder,
    empty_cell = "flag"
  )

  expect_true(any(plan$plan$orphan))
  expect_gt(plan$diagnostics$pop_orphan, 0)
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```bash
Rscript -e 'devtools::test(filter = "poststrat-plan")'
```

Expected: failure because `wf_plan_poststrat()` is missing.

- [ ] **Step 3: Implement validation and resolution**

Add `.wf_check_poststrat_args()`, `.wf_resolve_group()`, and
`.wf_redistribute()` to `R/poststrat-utils.R` using the reference implementation
as the algorithmic source. Keep these package-facing adjustments:

```r
.wf_check_poststrat_args <- function(sample, target, min_cell, ladder) {
  if (is.null(target$joint)) {
    wf_abort(
      "Target has no joint cell table. Rebuild with wf_target_population(..., keep_joint = TRUE).",
      "wf_error_schema"
    )
  }
  if (!inherits(ladder, "wf_collapse_ladder")) {
    wf_abort("`ladder` must be a wf_collapse_ladder object.", "wf_error_input")
  }
  if (missing(min_cell) || length(min_cell) != 1 || !is.finite(min_cell) ||
      min_cell < 1 || min_cell != as.integer(min_cell)) {
    wf_abort("`min_cell` must be a single integer >= 1.", "wf_error_input")
  }
  .require_cols(sample, c(target$dims, target$by), "sample")
  if (!identical(sort(ladder$dims), sort(target$dims))) {
    wf_abort(
      "Ladder dimensions do not match target dimensions.",
      "wf_error_schema",
      list(ladder = ladder$dims, target = target$dims)
    )
  }
  invisible(TRUE)
}
```

Create `R/poststrat-plan.R` with roxygen and:

```r
wf_plan_poststrat <- function(sample, target, min_cell, ladder,
                              granularity = c("adaptive", "province"),
                              empty_cell = c("redistribute", "flag", "error"),
                              id = NULL) {
  granularity <- match.arg(granularity)
  empty_cell <- match.arg(empty_cell)
  .wf_check_poststrat_args(sample, target, min_cell, ladder)
  dvars <- target$dims
  gkey <- .wf_group_keys(sample, target$by)

  plan_rows <- list()
  diag_rows <- list()
  for (g in intersect(names(target$joint), unique(gkey))) {
    sel <- gkey == g
    smat <- as.matrix(sapply(dvars, function(d) .chr(sample[[d]][sel])))
    if (is.null(dim(smat))) {
      smat <- matrix(smat, ncol = length(dvars), dimnames = list(NULL, dvars))
    }
    jdf <- target$joint[[g]]
    res <- .wf_resolve_group(smat, jdf, dvars, ladder, min_cell, granularity, g)
    if (any(res$orphan) && empty_cell == "redistribute") {
      res <- .wf_redistribute(res, min_cell, g)
    }
    plan_rows[[g]] <- data.frame(
      group = g,
      jdf[, dvars, drop = FALSE],
      pop = jdf$pop,
      n_sample = res$supp_final,
      ladder_level = res$level_used,
      resolved_cell = res$resolved,
      orphan = res$orphan,
      stringsAsFactors = FALSE
    )
    diag_rows[[g]] <- data.frame(
      group = g,
      n_cells_raw = nrow(jdf),
      n_cells_resolved = length(unique(res$resolved)),
      granularity_used = res$granularity_used,
      n_at_level0 = sum(res$level_used == 0),
      max_level_used = max(res$level_used),
      n_orphan = sum(res$orphan),
      pop_orphan = sum(jdf$pop[res$orphan]),
      stringsAsFactors = FALSE
    )
  }
  structure(
    list(
      plan = do.call(rbind, plan_rows),
      diagnostics = do.call(rbind, diag_rows),
      settings = list(min_cell = min_cell, granularity = granularity, empty_cell = empty_cell, ladder = ladder)
    ),
    class = "wf_poststrat_plan"
  )
}
```

Add `print.wf_poststrat_plan()` and `summary.wf_poststrat_plan()` that return
the diagnostics table invisibly and as a data frame, respectively.

- [ ] **Step 4: Run tests to verify they pass**

Run:

```bash
Rscript -e 'devtools::test(filter = "poststrat-plan|poststrat-ladder|poststrat-target")'
```

Expected: planner, ladder, and target tests pass with 0 failures.

- [ ] **Step 5: Commit**

```bash
git add R/poststrat-utils.R R/poststrat-plan.R tests/testthat/helper-fixtures.R tests/testthat/test-poststrat-plan.R
git commit -m "feat: add poststrat planning"
```

---

### Task 4: Post-Stratification Executor

**Files:**
- Create: `R/poststrat.R`
- Modify: `R/rake.R`
- Test: `tests/testthat/test-poststrat-exec.R`

- [ ] **Step 1: Write the failing tests**

Create `tests/testthat/test-poststrat-exec.R` with:

```r
test_that("wf_poststrat matches joint cell population totals", {
  fixture <- make_poststrat_fixture()

  weights <- wf_poststrat(
    fixture$sample,
    fixture$target,
    min_cell = 1,
    ladder = fixture$ladder,
    id = "id"
  )

  expect_s3_class(weights, "wf_weights")
  expect_equal(weights$provenance$method, "poststrat")
  expect_true(all(weights$data$weight >= 0))
  expect_equal(sum(weights$data$weight), fixture$target$groups$A$total, tolerance = 1e-8)
  expect_true(all(c("cell_report", "collapse_map") %in% names(weights)))
  expect_equal(max(weights$log$total_dev), 0, tolerance = 1e-8)
})

test_that("wf_poststrat preserves initial weight ratios within resolved cells", {
  fixture <- make_poststrat_fixture()
  fixture$sample$base_w <- seq_len(nrow(fixture$sample))

  weights <- wf_poststrat(
    fixture$sample,
    fixture$target,
    min_cell = 2,
    ladder = fixture$ladder,
    init_weight = "base_w",
    id = "id"
  )

  merged <- merge(weights$data, fixture$sample[, c("id", "base_w")], by = "id")
  by_cell <- split(merged, merged$resolved_cell)
  checked <- FALSE
  for (d in by_cell) {
    if (nrow(d) >= 2 && all(d$weight > 0)) {
      expect_equal(d$weight[1] / d$weight[2], d$base_w[1] / d$base_w[2], tolerance = 1e-8)
      checked <- TRUE
      break
    }
  }
  expect_true(checked)
})

test_that("wf_poststrat can flag or reject empty cells", {
  fixture <- make_poststrat_fixture()
  fixture$sample <- fixture$sample[fixture$sample$gender == "female", ]

  expect_error(
    wf_poststrat(
      fixture$sample,
      fixture$target,
      min_cell = 1,
      ladder = fixture$ladder,
      empty_cell = "error"
    ),
    class = "wf_error_feasibility"
  )

  flagged <- wf_poststrat(
    fixture$sample,
    fixture$target,
    min_cell = 1,
    ladder = fixture$ladder,
    empty_cell = "flag"
  )
  expect_lt(sum(flagged$data$weight), fixture$target$groups$A$total)
  expect_gt(flagged$log$total_dev, 0)
})
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```bash
Rscript -e 'devtools::test(filter = "poststrat-exec")'
```

Expected: failure because `wf_poststrat()` is missing.

- [ ] **Step 3: Implement executor**

Create `R/poststrat.R` with roxygen and implement `wf_poststrat()` from the
reference implementation, with these required package adjustments:

- return `data` columns `id`, `group`, `resolved_cell`, `weight`, `feature`;
- return `log` with `iterations = NA_integer_`, `converged = TRUE`, and
  `trimmed = 0L` compatibility columns so `wf_diagnose()` can merge logs;
- set `achieved = NULL` because poststrat checks joint cells through
  `cell_report`;
- fill `cell_report$redistributed_to` and `cell_report$pop_moved` by matching
  the orphan raw cell key from `res$redist_log`;
- under `empty_cell = "flag"`, do not raise an internal error when the realized
  total is lower by the flagged population; record the relative shortfall.

The post-condition is:

```r
expected_total <- target$groups[[g]]$total - pop_flagged
realized <- sum(w)
dev <- if (expected_total > 0) abs(realized - expected_total) / expected_total else 0
if (empty_cell != "flag" && dev > tol) {
  wf_abort(
    sprintf("Group '%s': province-total constraint violated (realized %.6g vs target %.6g, dev %.2e). This indicates an internal bug.", g, realized, expected_total, dev),
    "wf_error_internal",
    list(group = g, dev = dev)
  )
}
```

Update `print.wf_weights()` in `R/rake.R` so existing raking output is preserved
unless `x$provenance$method == "poststrat"`:

```r
if (!is.null(x$provenance$method) && x$provenance$method == "poststrat") {
  cat(sprintf("<wf_weights>  %d unit(s) in %d group(s); method: poststrat\n", nrow(x$data), nrow(x$log)))
  cat(sprintf("  weight range [%.4g, %.4g]; elapsed %.2fs\n", min(x$data$weight), max(x$data$weight), x$provenance$elapsed))
  cat(sprintf("  cells: %d raw -> %d resolved (median); groups using province granularity: %d\n", stats::median(x$log$n_cells_raw), stats::median(x$log$n_cells_resolved), sum(x$log$granularity_used == "province")))
  cat(sprintf("  max group-total deviation: %.2e\n", max(x$log$total_dev)))
  return(invisible(x))
}
```

- [ ] **Step 4: Run tests to verify they pass**

Run:

```bash
Rscript -e 'devtools::test(filter = "poststrat-exec|poststrat-plan|poststrat-ladder|poststrat-target|rake-diagnostics")'
```

Expected: poststrat and existing raking diagnostics tests pass with 0 failures.

- [ ] **Step 5: Commit**

```bash
git add R/poststrat.R R/rake.R tests/testthat/test-poststrat-exec.R
git commit -m "feat: add poststrat execution"
```

---

### Task 5: Diagnostics, Documentation, and Exports

**Files:**
- Modify: `R/diagnostics.R`
- Modify: `README.md`
- Modify: `DESCRIPTION`
- Modify: `NAMESPACE`
- Modify: `man/*.Rd`
- Test: `tests/testthat/test-rake-diagnostics.R`
- Test: `tests/testthat/test-poststrat-exec.R`

- [ ] **Step 1: Write failing compatibility tests**

Append to `tests/testthat/test-poststrat-exec.R`:

```r
test_that("wf_diagnose supports poststrat weights", {
  fixture <- make_poststrat_fixture()

  weights <- wf_poststrat(
    fixture$sample,
    fixture$target,
    min_cell = 1,
    ladder = fixture$ladder,
    id = "id"
  )
  diag <- wf_diagnose(weights)

  expect_s3_class(diag, "wf_diagnostics")
  expect_true(all(c("ess", "deff", "verdict") %in% names(diag$table)))
  expect_true(all(diag$table$converged))
})
```

Append to `tests/testthat/test-rake-diagnostics.R`:

```r
test_that("raking print and diagnose remain compatible with poststrat changes", {
  fixture <- make_weightflow_fixture()
  weights <- wf_rake(fixture$sample, fixture$target, id = "id", tol = 1e-8)

  expect_output(print(weights), "<wf_weights>")
  diag <- wf_diagnose(weights, target = fixture$target)
  expect_true(all(c("iterations", "converged", "trimmed") %in% names(diag$table)))
  expect_true(all(diag$table$converged))
})
```

- [ ] **Step 2: Run tests to verify they fail where compatibility is incomplete**

Run:

```bash
Rscript -e 'devtools::test(filter = "poststrat-exec|rake-diagnostics")'
```

Expected: failure if diagnostics still assumes raking-only fields.

- [ ] **Step 3: Implement diagnostics compatibility and package metadata**

Modify `wf_diagnose()` to merge optional log columns safely:

```r
log_cols <- intersect(c("group", "iterations", "converged", "trimmed"), names(w$log))
tab <- merge(tab, w$log[, log_cols, drop = FALSE], by = "group", all.x = TRUE)
if (!"iterations" %in% names(tab)) tab$iterations <- NA_integer_
if (!"converged" %in% names(tab)) tab$converged <- TRUE
if (!"trimmed" %in% names(tab)) tab$trimmed <- 0L
```

Only compute `margin_maxerr` when both `target` and `w$achieved` are present.

Update `DESCRIPTION`:

```text
Version: 0.2.0
```

Run roxygen:

```bash
Rscript -e 'devtools::document()'
```

Expected: `NAMESPACE` exports `wf_collapse_ladder`, `wf_plan_poststrat`,
`wf_poststrat`, and registers `print.wf_poststrat_plan` plus
`summary.wf_poststrat_plan`.

Update `README.md` with a concise post-strat example using only
`weightflow_example` or the test fixture pattern.

- [ ] **Step 4: Run focused verification**

Run:

```bash
Rscript -e 'devtools::test(filter = "poststrat|rake-diagnostics|dims-target")'
```

Expected: focused suite passes with 0 failures.

- [ ] **Step 5: Commit**

```bash
git add DESCRIPTION NAMESPACE R/diagnostics.R README.md man tests/testthat/test-poststrat-exec.R tests/testthat/test-rake-diagnostics.R
git commit -m "chore: document poststrat API"
```

---

### Task 6: Full Verification and Repository Audit

**Files:**
- Modify only files required by verification output.

- [ ] **Step 1: Run full tests**

Run:

```bash
Rscript -e 'devtools::test()'
```

Expected: all tests pass, 0 failures.

- [ ] **Step 2: Audit language policy**

Run:

```bash
rg -n --pcre2 '\p{Han}' -g '!README.zh-CN.md' -g '!private-data/**' .
```

Expected: no output.

- [ ] **Step 3: Audit private-data policy**

Run:

```bash
git status --short --ignored
git ls-files | rg 'private-data|\\.xlsx$|\\.RData$|Rcheck|\\.tar\\.gz$'
```

Expected: tracked-file query has no output. Ignored output may list local
private/build artifacts.

- [ ] **Step 4: Build package tarball**

Run:

```bash
R CMD build .
```

Expected: creates `weightflow_0.2.0.tar.gz`.

- [ ] **Step 5: Check package tarball**

Run:

```bash
R CMD check --no-manual weightflow_0.2.0.tar.gz
```

Expected: status OK. Repository index access warnings are acceptable only if
they are network-related and the final status is OK.

- [ ] **Step 6: Audit tarball contents**

Run:

```bash
tar -tzf weightflow_0.2.0.tar.gz | rg 'private-data|source|xlsx|RData|README.zh-CN|data-raw'
```

Expected: no output.

- [ ] **Step 7: Commit any final verification-only fixes**

If verification required edits, run focused tests again and commit:

```bash
git add <changed-files>
git commit -m "test: verify poststrat package checks"
```

If no edits were required, do not create an empty commit.

---

## Self-Review

- Spec coverage: the plan covers joint target storage, collapse ladders,
  planning, execution, audit outputs, diagnostics compatibility, documentation,
  package versioning, and final verification.
- Placeholder scan: no task uses open placeholders; each test and command is
  concrete.
- Type consistency: public names match the approved spec:
  `wf_collapse_ladder()`, `wf_plan_poststrat()`, `wf_poststrat()`, and
  `keep_joint`.
