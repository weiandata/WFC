% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report.R
\name{wf_report}
\alias{wf_report}
\title{Build a weighting quality report}
\usage{
wf_report(
  w,
  target = NULL,
  audience = c("manager", "analyst", "decision", "statistician"),
  lang = NULL,
  output = c("object", "markdown", "html"),
  file = NULL
)
}
\arguments{
\item{w}{A \code{wf_weights}, \code{wf_blend_result}, \code{wf_safe_workflow}, or
\code{wf_impact} object.}

\item{target}{Optional \code{wf_target} for margin-residual diagnostics.}

\item{audience}{Report projection: \code{"manager"}/\code{"decision"} for a compact
decision view or \code{"analyst"}/\code{"statistician"} for full detail.}

\item{lang}{Output language. Resolution follows explicit argument,
\code{options(wfc.lang)}, session locale, then English fallback.}

\item{output}{Return a structured object, Markdown, or standalone HTML.}

\item{file}{Optional output path for Markdown or HTML.}
}
\value{
A \code{wf_quality_report} for \code{output = "object"}; otherwise a rendered
character string, or invisibly the structured report when \code{file} is used.
}
\description{
Creates one structured dossier with manager and analyst projections. It can
report calibrated/composed weights or a blend result and render the same
payload as Markdown or dependency-free HTML.
}
\examples{
\dontrun{
# After wf_execute_plan() returns locked weights:
wf_report(locked, audience = "decision")
wf_report(locked, audience = "statistician")
}
}
