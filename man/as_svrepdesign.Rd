% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interoperability.R
\name{as_svrepdesign}
\alias{as_svrepdesign}
\title{Convert WFC replicate weights to a survey replicate design}
\usage{
as_svrepdesign(r, data, id = "id", degf = NULL, ...)
}
\arguments{
\item{r}{A \code{wf_replicate_weights} object.}

\item{data}{Non-empty analysis data frame containing the units in \code{r}.}

\item{id}{Unit-ID column in \code{data}; matched to \code{r$base$id}.}

\item{degf}{Optional survey design degrees of freedom.}

\item{...}{Additional arguments passed to \code{survey::svrepdesign()}. WFC owns
the variables, base/replicate weights, replication type and scales,
combined-weight setting, and MSE setting.}
}
\value{
A standard \code{svyrep.design} object whose variables include the aligned
\code{.wf_weight} base-weight column.
}
\description{
Aligns recalibrated WFC replicate weights to analysis data by exact unit ID
and creates a standard \code{survey::svrepdesign()} object. The returned design
uses full-estimate-centered (\code{mse = TRUE}) variance so its estimates reproduce
\code{\link[=wf_variance]{wf_variance()}}.
}
